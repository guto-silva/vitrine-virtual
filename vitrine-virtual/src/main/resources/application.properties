# Usu�rio do banco
spring.datasource.username = root

# Senha do banco
spring.datasource.password = root

# URL do banco
spring.datasource.url = jdbc:mysql://localhost:3306/db_vitrine?useTimezone=true&serverTimezone=UTC

# Dialeto do banco
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect